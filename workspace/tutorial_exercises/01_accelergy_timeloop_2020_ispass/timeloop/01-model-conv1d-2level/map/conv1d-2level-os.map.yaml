mapping:
  - target: MainMemory
    type: temporal
    factors: R=1 P=1920 # R 维度不分块(因子为1)；P 维度分成 16 块（分16次从main memory读取数据到buffer）。
    permutation: PR
    
  - target: Buffer
    type: temporal
    factors: R=3 P=1
    permutation: RP
    
