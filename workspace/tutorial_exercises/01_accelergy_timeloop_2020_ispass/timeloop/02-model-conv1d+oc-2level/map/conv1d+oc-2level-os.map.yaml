mapping:
  - target: MainMemory
    type: temporal
    # factors: R=1 P=16 K=32 
    factors: [R=1, P=16, K=32]  # 分32次从main memory读入K到buffer，每次一块
    permutation: [P, R, K]
    
  - target: Buffer
    type: temporal
    factors: [R=3, P=1, K=1]  # 不分块，实际上就是一次读入一个K
    permutation: RPK
    
